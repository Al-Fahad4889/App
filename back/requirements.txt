streamlit
streamlit-webrtc>=0.47.0
mediapipe

Standard torch with correct index-url syntax for requirements.txt

--extra-index-url https://download.pytorch.org/whl/cpu
torch
torchvision

Use the simpler torch-geometric install to avoid C++ compilation errors

torch-geometric

Fix for numpy 2.0 breaking changes

numpy>=1.24.0,<2.0.0
opencv-python-headless
aiortc

Pin tornado to fix the BaseAsyncIOLoop error

tornado==6.3.3
